<div class="p-4 sm:ml-64 w-full ">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Roles</h1>
    <%= link_to "New Role", new_role_path, class: "rounded-lg py-3 px-5 bg-slate-600 text-white block font-medium" %>
  </div>

  <div id="categories" class="min-w-full">
    <%= render 'table', records: @roles %>
  </div>
  <%= render "pagination/pagination", records: @roles %>
</div>