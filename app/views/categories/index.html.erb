<div class="p-4 sm:ml-64 w-full ">

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Categories</h1>
    <%= link_to "New category", new_category_path, class: "rounded-lg py-3 px-5 bg-slate-600 text-white block font-medium" %>
  </div>

  <div id="categories" class="min-w-full">
    <%= render 'table', categories: @categories %>
  </div>
  <%= render "pagination/pagination", records: @categories %>
</div>
